#

echo "not implemented, uo to now connect into the same database scanner server'

# sudo mkdir -p /data/pyfinder


#m=$(docker stop mongo); echo "Stopped $m container"
#
#m=$(docker rm mongo); echo "Removed $m container"
#
#m=$(docker run --name=mongo -v /data/pyfinder:/data/db -d mongo)
#IP="$(docker inspect --format '{{ .NetworkSettings.IPAddress }}' mongo)"
#
#echo "Running $m on $IP "

